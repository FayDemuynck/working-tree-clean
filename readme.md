# Git fixes

## Git fixes Guidance
### Combineer de volgende commando's
- git checkout .
- git clean --force
- git reset
- git revert

### Let op voor invasieve commando's
  - git push --force
  - git reset --hard
